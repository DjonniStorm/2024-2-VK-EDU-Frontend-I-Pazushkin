export const SearchChatInput = () => {
    return (
        <input
            placeholder="Поиск"
            type="text"
            autoComplete="on"
            spellCheck="true"
        />
    );
}